import React from "react";

const Navbar = () => {
  return (
    <nav className="w-full p-3 text-center font-bold text-yellow-100 bg-emerald-500 text-xl flex items-center justify-center">
      <div>
        <span className="line-through text-4xl">S</span>
        <span className="pt-2">Task</span>
      </div>
    </nav>
  );
};

export default Navbar;
